{
  "Pascal-Case Class": {
    "prefix": "dclass",
    "body": [
      "export class ${TM_FILENAME_BASE/(^|[-_])([a-zA-Z])/${2:/upcase}/g} {",
      "\t$0",
      "}"
    ],
    "description": "Insert a class named from the current filename (kebab/snake → PascalCase)"
  },
  "Pascal-Case Frozen Enum": {
    "prefix": "denum",
    "body": [
      "/** @enum {string} */",
      "export const ${TM_FILENAME_BASE/(^|[-_])([a-zA-Z])/${2:/upcase}/g} = Object.freeze({",
      "\t$0",
      "});"
    ],
    "description": "Insert a frozen-object enum named from the current filename (kebab/snake → PascalCase)"
  }
}
